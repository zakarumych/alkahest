
formula Empty;

formula Foo = {
    a: u32,
    b: u32,
};

formula Bar = {
    x: Foo,
    y: u64,
};

formula Baz a = {
    items: [a; 4],
};

formula Either a b = |A(a) |B(b);

formula Side = |Left { a: u8 } |Right { b: u16 } ;

formula X = {
    never: ?!,
};
